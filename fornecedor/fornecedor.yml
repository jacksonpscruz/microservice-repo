spring:

  jpa:
    show-sql: true
    generate-ddl: true
    database: mysql
 #   hibernate:
 #     ddl-auto: create-drop

  datasource:
    password: root
    username: root
    url: jdbc:mysql://localhost:3306/fornecedor
#    initialization-mode: always      
